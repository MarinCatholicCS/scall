/** @type {import('next').NextConfig} */
const nextConfig = {
  // The existing /api/*.ts files stay as plain Vercel functions
  // (they coexist with Next.js routes — no migration needed).
};

export default nextConfig;
